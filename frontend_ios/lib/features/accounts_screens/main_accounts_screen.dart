import 'package:flutter/material.dart';

class MainAccountsScreen extends StatelessWidget {
  const MainAccountsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is the banks screen"),
    );
  }
}